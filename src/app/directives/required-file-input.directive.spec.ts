import { RequiredFileInputDirective } from './required-file-input.directive';

describe('RequiredFileInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredFileInputDirective();
    expect(directive).toBeTruthy();
  });
});
