import { AsideMenuTogglerDirective } from './aside-menu-toggler.directive';

describe('AsideMenuTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideMenuTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
